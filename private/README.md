# Private evaluation data

This directory is local-only staging for raw runs, prompts, responses, transcripts, logs,
and private source. Its contents are ignored except for this policy file.

Do not commit or publish files from this directory. Convert raw runs into schema-valid,
reviewed aggregates under `data/public/` only after removing private or identifying data.
