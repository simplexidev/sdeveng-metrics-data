name: CI

on:
  push:
    branches: [main, develop/v3.0.0]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: simplexidev/sdeveng-metrics-tooling
          ref: develop/v3.0.0
          path: dependencies/tooling
      - uses: actions/checkout@v4
        with:
          repository: simplexidev/sdeveng-metrics-dashboard
          ref: develop/v3.0.0
          path: dependencies/dashboard
      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: dependencies/tooling/global.json
      - run: dotnet restore dependencies/tooling/SdevEng.Metrics.slnx
      - run: dotnet build dependencies/tooling/SdevEng.Metrics.slnx --no-restore
      - run: dotnet run --project dependencies/tooling/src/SdevEng.Metrics --no-build -- publish-pages dependencies/dashboard public _site
      - run: git diff --check
